name: himedeco-poll

on:
  schedule:
    - cron: "3 * * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "確認のみ（自動返信しない）"
        type: boolean
        default: true
      verify_order:
        description: "一覧の並び順の検証（全員分を調べる）"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: himedeco-poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 62
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests beautifulsoup4
      - name: Poll
        env:
          REPLY_DRY_RUN: ${{ github.event_name == 'schedule' && 'false' || (inputs.dry_run == false && 'false' || 'true') }}
          LOOP_MINUTES: ${{ github.event_name == 'schedule' && '57' || '0' }}
          VERIFY_ORDER: ${{ inputs.verify_order && '1' || '' }}
          CITYHEAVEN_SHOP_CREDS: ${{ secrets.CITYHEAVEN_SHOP_CREDS }}
        run: python3 poll.py
      - name: Save status
        if: always()
        run: |
          git config user.name "poll-bot"
          git config user.email "actions@github.com"
          git add status.json
          git diff --cached --quiet || { git commit -m "status" && git push; } || true
